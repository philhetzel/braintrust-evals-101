To run:

```
export BRAINTRUST_API_KEY=<YOUR_API_KEY>
braintrust eval src/evals/02_use_braintrust_objects/use_braintrust_objects.py 
```

This uses the NumericDiff scorer from autoevals whish is a heuristic scorer that measures how close a numeric output is to an expected value
